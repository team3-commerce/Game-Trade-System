# PRD — Game Trade System

> 게임 아이템 거래소 (Product Requirements Document)
>
> 이 문서는 현재 구현된 코드(`dev` 브랜치)를 역으로 정리한 제품 요구사항 문서입니다.
> 비즈니스 규칙 원문은 [BUSINESS.md](BUSINESS.md), API 상세는 [API.md](API.md)를 참고하세요.

---

## 1. 제품 개요

### 1.1 한 줄 정의

가상 게임 내 아이템을 유저 간에 사고팔 수 있는 웹 기반 거래소(마켓플레이스) 서비스.

### 1.2 배경과 목적

이 프로젝트는 상용 서비스가 아니라 **백엔드 기술 학습을 목적으로 한 팀 프로젝트**입니다.
따라서 제품 요구사항과 별개로 다음 학습 목표가 요구사항 설계에 반영되어 있습니다.

| 학습 목표 | 제품 요구사항에 반영된 형태 |
|---|---|
| 동시성 제어 | 선착순 쿠폰, 동일 상품 동시 구매, 중복 상품 등록 방지 |
| 캐싱 전략 | 대량 조회 API(아이템 도감, 거래소 목록, 인벤토리) |
| 실시간 통신 | 구매자–판매자 1:1 채팅 |
| 성능 측정 | 동일 기능의 다중 버전 API(V1~V5) 비교 |
| 배포 자동화 | GitHub Actions → Docker Hub → AWS EC2 |

이 목적 때문에 **같은 기능이 여러 버전의 엔드포인트로 동시에 존재**합니다. 이는 버그가 아니라
의도된 설계이며, 성능 비교 실험의 대조군 역할을 합니다.

### 1.3 범위 밖 (Out of Scope)

- 실제 화폐 결제 / PG 연동 (게임 머니만 사용)
- 프론트엔드 웹/앱 클라이언트 (채팅 테스트용 `chat.html` 제외)
- 실제 게임 클라이언트와의 연동
- 다국어 지원, 알림(푸시/메일)
- 관리자 백오피스 UI (관리자용 API만 존재)

---

## 2. 사용자와 역할

| 역할 | 설명 | 권한 |
|---|---|---|
| 비회원 | 가입/로그인만 가능 | 인증 API 화이트리스트 |
| 일반 회원 (`USER`) | 거래소 이용의 주체. 상황에 따라 판매자/구매자가 됨 | 자신의 자원에 대한 CRUD |
| 관리자 (`ADMIN`) | 쿠폰 정책 발행, 회원 정지, 상품 강제 취소 | `/api/*/admin/**` |

회원은 고정된 "판매자/구매자" 역할이 아니라, 행위에 따라 그때그때 결정됩니다.
채팅방에서만 `BUYER` / `SELLER` 역할이 명시적으로 부여됩니다.

### 2.1 회원 상태

| 상태 | 진입 조건 | 결과 |
|---|---|---|
| `ACTIVE` | 가입 / 재로그인 | 정상 이용 |
| 비활성(장기 미접속) | 30일 미접속 (일 1회 배치) | 로그인 시 즉시 `ACTIVE` 복귀 |
| 정지 | 관리자 조치 | 로그인 차단 (사유 저장) |
| 탈퇴 | 본인 요청 | 1년 후 데이터 완전 삭제 |

장기 미접속과 관리자 정지는 **별도로 관리**됩니다. 정지 상태가 미접속 배치로 덮어써지면 안 됩니다.

탈퇴 조건: 거래소에 판매중인 상품과 수령 대기 항목이 하나도 없어야 합니다.
단, 탈퇴 전에 판매한 상품을 구매자가 아직 수령하지 않았다면 구매자는 여전히 수령할 수 있어야 합니다.

---

## 3. 핵심 기능 요구사항

### 3.1 인증 / 인가

**F-AUTH-01 이메일 회원가입**
- 이메일과 닉네임은 각각 전역 유니크.
- 가입 시 지갑(`Wallet`) 생성, 기본 아이템 지급, 가입 축하 쿠폰(10,000 머니) 자동 지급.

**F-AUTH-02 로그인 / 토큰**
- JWT Access Token + Refresh Token 방식. 서버는 세션을 유지하지 않음(STATELESS).
- 로그아웃 시 토큰을 블랙리스트에 등록해 남은 유효기간 동안 사용 차단.
- Refresh Token으로 재발급(reissue) 가능.

**F-AUTH-03 소셜 로그인 (Google / Kakao / GitHub)**
- 한 회원이 여러 소셜 계정을 동시에 연동할 수 있어야 함(1:N).
- 어떤 수단으로 로그인하든 **동일한 회원 식별자**를 반환해 지갑/인벤토리/주문 이력이 유지되어야 함.
- 이미 같은 이메일의 일반 회원이 있으면 신규 가입이 아니라 기존 계정 연동으로 유도.
- 소셜 서비스 자체를 탈퇴한 사용자는 재로그인 시 인증에 실패하므로,
  "연동된 소셜 계정을 찾을 수 없습니다. 일반 로그인을 이용해 주세요." 형태의 안내를 제공.

**F-AUTH-04 소셜 → 통합 계정 전환**
- 소셜로만 가입한 회원은 비밀번호가 없는 상태. '비밀번호 설정'을 거치면 일반 로그인도 가능해짐.
- 비밀번호가 이미 있으면 재설정 요청은 거부(별도 변경 API 사용).

**F-AUTH-05 소셜 연동 해제**
- 서비스 탈퇴가 아니라 해당 소셜 제공자와의 연결만 끊는 동작.
- 다른 로그인 수단이 하나도 남지 않게 되는 연동 해제는 **차단**해야 함(계정 분실 방지).

**F-AUTH-06 가상 이메일 정책**
- 소셜 제공자가 이메일을 주지 않는 경우(Kakao 비즈앱 미설정, GitHub private email)
  `{소셜고유ID}@{provider}.com` 형태의 가상 이메일을 생성해 유니크 제약을 만족시킴.
- 학습 목적의 단순화 정책이며 실무 권장 방식이 아님을 문서에 명시.

**F-AUTH-07 회원 정보 관리**
- 내 정보 조회, 닉네임 변경(중복 불가), 비밀번호 변경, 회원 탈퇴.
- 관리자에 의한 회원 정지.

### 3.2 아이템 / 인벤토리

**F-ITEM-01 아이템 도감**
- 아이템 종류: `장비`, `소비`, `기타`. 아이템 이름은 유니크. 모든 아이템은 교환 가능.
- 아이템 자체에는 고정 가격이 없음(가격은 거래소 등록 시 판매자가 결정).
- 전체 목록 조회(페이지네이션) 및 단건 조회. 이름 기반 검색 지원.

**F-INV-01 인벤토리**
- 인벤토리 크기는 무제한.
- 소진된 아이템은 삭제하지 않고 수량 `0`으로 표기.
- 회원가입 시 기본 아이템 지급.
- 내 인벤토리 목록 조회 / 단건 조회.

### 3.3 거래소 (Market Listing)

**F-MKT-01 상품 등록 (판매자)**
- 한 번에 한 품목만 등록. 같은 아이템 여러 개는 수량으로 표현.
- 가격은 개당 가격 또는 총액 중 하나로 입력할 수 있어야 함(둘 다 저장).
- 판매 만료 시간은 12/24/48시간 중 선택.
- **동일 인벤토리 아이템을 중복 등록할 수 없어야 함** — 동시 요청 상황에서도 보장되어야 함.
- 보유 수량을 초과해 등록할 수 없음.

**F-MKT-02 상품 조회 (전체)**
- 만료되지 않은 판매중 상품만 노출.
- 키워드 검색, 총액 정렬(asc/desc), 만료일 정렬 지원.
- 잘못된 정렬 값은 예외가 아니라 **무시**(정렬 조건에서 제외).
- 페이지 크기 10 고정.

**F-MKT-03 인기 검색어**
- 검색 횟수 상위 5개 키워드 조회.
- 특정 접두어로 시작하는 인기 검색어 조회(자동완성 용도).
- **하루 단위로 순위 초기화.**

**F-MKT-04 상품 상세 조회**
- 개당 금액, 총 금액, 수량, 아이템 타입, 등록 시각, 만료 시각을 확인할 수 있어야 함.

**F-MKT-05 내 상품 조회 / 취소**
- 판매 취소는 `SELLING` 상태에서만 가능. 이미 팔린 상품은 취소 불가.
- 취소 시 아이템은 즉시 인벤토리로 돌아가지 않고 **수령 대기 상태**가 됨.

**F-MKT-06 관리자 강제 취소**
- 관리자는 임의 상품을 취소할 수 있어야 함.

**F-MKT-07 자동 만료**
- 만료 확인은 00시, 06시, 12시, 18시에 수행.
- 등록 시각 기준 최소 노출 시간을 보장 — 예: 03시에 등록한 12시간 상품은 12시가 아니라
  18시에 만료(실 노출 15시간). 노출 시간이 선택한 시간보다 짧아지면 안 됨.
- 만료된 상품도 즉시 반환이 아니라 수령 대기 상태로 전환.

### 3.4 구매와 정산

**F-ORD-01 상품 구매**
- 부분 구매 불가. 등록된 상품 전체를 구매해야 함.
- 잔액 부족 시 구매 실패.
- 이미 팔린 상품에 대한 구매 요청은 실패해야 함 — **동시 요청 시에도 이중 판매가 발생하면 안 됨.**
- 구매 성공 시 한 트랜잭션 안에서: 주문 생성 → 지갑 차감 및 이력 기록 → 양측 수령 대기 생성 → 상품 상태 `SOLD`.

**F-ORD-02 주문 스냅샷**
- 주문 시점의 상품명/가격/수량을 별도로 보존해, 이후 원본이 바뀌어도 이력이 훼손되지 않아야 함.

**F-ORD-03 구매/판매 내역 조회**
- 내가 산 목록, 내가 판 목록을 각각 조회.

**F-PEND-01 수령 대기 자산 (PendingAsset)**
- 거래 결과(대금 / 아이템 / 취소·만료 반환분)는 즉시 지급되지 않고 **수령 대기 목록**에 쌓임.
- 회원이 명시적으로 수령해야 지갑 또는 인벤토리에 반영됨.
- **제한 시간 내에 수령하지 않으면 소멸**(스케줄러가 처리).
- 대기 항목 유형: 판매 성공 대금, 구매 성공 아이템, 판매 취소 반환, 만료 반환.

**F-WAL-01 지갑**
- 잔액 조회. 모든 잔액 변동은 변동 사유(주문 / 쿠폰 사용 등)와 변동 후 잔액 스냅샷과 함께 기록되어야 함.

### 3.5 쿠폰

**F-CPN-01 쿠폰 정책 발행 (관리자)**
- 정책명은 유니크. 지급 머니 금액, 발급 유형, 정책 기간, 쿠폰 유효기간을 설정.
- 발급 유형: 가입 자동 지급 / 선착순.
- 선착순 정책은 총 수량이 필수. 가입 자동 지급 정책은 하나만 존재할 수 있음.

**F-CPN-02 선착순 쿠폰 발급 신청**
- 총 수량을 초과해 발급되면 안 됨 — **대량 동시 요청 상황에서 발급 수량과 소진 수량이 정확히 일치해야 함.**
- 동일 회원이 같은 정책으로 중복 발급받을 수 없음.
- 소진 시 명확한 실패 응답(품절).

**F-CPN-03 쿠폰 조회**
- 정책 목록 조회 (총 수량 / 잔여 수량 포함, 발급 유형별 필터).
- 내 쿠폰 전체 조회 (미사용 / 사용 / 만료 상태별 필터), 단건 조회는 상태 무관.

**F-CPN-04 쿠폰 사용**
- 사용 시 상태를 `USED`로 전이하고 사용 이력을 남김.
- 동시에 지갑에 머니가 증가하고 지갑 변동 이력이 생성되어야 함(원자적).
- 이미 사용했거나 만료된 쿠폰은 사용 불가.

**F-CPN-05 쿠폰 만료**
- 일 1회 만료일이 지난 쿠폰을 `EXPIRED`로 전이하고 이력을 남김.

**F-CPN-06 사용 내역 조회**
- 사용 또는 만료된 쿠폰만 조회 대상.

### 3.6 실시간 채팅

**F-CHAT-01 채팅방 생성**
- 구매자가 상품 상세에서 "판매자와 채팅하기"로만 생성 가능.
- 시작한 사람은 `BUYER`, 상대는 자동으로 `SELLER`로 등록.
- `SELLING` 상태 상품에 대해서만 생성 가능.
- 같은 구매자 + 같은 상품 조합의 채팅방은 중복 생성 불가(같은 판매자의 다른 상품은 별개).

**F-CHAT-02 채팅방 목록**
- 내가 참여한 방만 조회. 전체 / 구매(내가 BUYER) / 판매(내가 SELLER) 탭으로 구분.

**F-CHAT-03 메시지 송수신**
- 실시간 전송. 다중 서버 환경에서도 같은 방의 참여자에게 전달되어야 함.
- 전송 시마다 상품 판매 상태를 확인하고, `SELLING`이 아니면 **메시지를 저장하지 않고**
  상태별 안내 시스템 메시지로 대체:
  - `SOLD` → "이 상품은 판매 완료된 상품입니다."
  - `CLAIMED` → "이 상품은 거래 완료된 상품입니다."
  - `CANCELLED` → "이 상품은 취소된 상품입니다."
  - `EXPIRED` → "이 상품은 판매 기간이 만료된 상품입니다."

**F-CHAT-04 메시지 조회**
- 커서 기반 페이지네이션. 최초/재연결 시 최신 50개, 이전 더 보기 시 마지막 ID 기준 이전 50개.

**F-CHAT-05 입퇴장**
- 입퇴장 시 "OOO님이 입장/퇴장했습니다" 시스템 메시지를 실시간 전달하되 **DB에 저장하지 않음.**
- `SELLING`이 아닌 방은 입력창 비활성화, 입퇴장 메시지도 보내지 않음.
- 퇴장 트리거: 다른 방 선택, 로그아웃, 창 닫기/새로고침.

### 3.7 운영 / 개발 지원

**F-OPS-01 디버그 API**
- `prod` 프로파일이 아닐 때만 활성. 임의 회원에게 아이템 지급 등 테스트 편의 기능.

**F-OPS-02 더미 데이터 시딩**
- 부하 테스트를 위해 회원/아이템/인벤토리/거래소 상품을 대량(수십만 건) 생성할 수 있어야 함.
- 생성 범위를 모드로 선택 가능하며, 거래소 상품 수는 인벤토리 수를 초과할 수 없음.

---

## 4. 비기능 요구사항

### 4.1 데이터 정합성

| 항목 | 요구 수준 |
|---|---|
| 선착순 쿠폰 발급 수 | 정책 총 수량과 정확히 일치. 초과 발급 0건 |
| 쿠폰 발급 수 ↔ 소진 수량 | 항상 동일 (부분 갱신으로 어긋나면 안 됨) |
| 동일 상품 이중 판매 | 발생 0건 |
| 동일 인벤토리 아이템 중복 등록 | 발생 0건 |
| 지갑 잔액 | 모든 변동에 대응하는 이력이 존재해야 함 |

### 4.2 성능

- 대량 조회 API(아이템 도감, 거래소 목록, 인벤토리)는 캐시 적용 전후를 k6로 측정해
  개선 폭을 정량적으로 확인할 수 있어야 함.
- 인기 검색어 조회는 DB 집계가 아닌 인메모리 자료구조 기반으로 상수 시간에 가깝게 동작.
- 부하 테스트 결과는 InfluxDB에 적재하고 Grafana 대시보드로 시각화.

### 4.3 보안

- 비밀번호는 BCrypt 해시로만 저장.
- 인증 실패(401) / 권한 없음(403)도 일반 API와 동일한 응답 포맷으로 반환.
- 시크릿(DB 비밀번호, JWT 서명키, OAuth 클라이언트 시크릿)은 환경변수로 주입.

### 4.4 응답 규약

- 모든 API는 공통 응답 래퍼로 감싼 형태를 반환. 목록형은 페이지 정보를 포함한 래퍼 사용.
- 도메인 에러는 코드/HTTP 상태/메시지가 한 곳에서 관리되어 응답이 일관되어야 함.

### 4.5 운영

- `main`/`dev` push 시 자동으로 이미지가 빌드되어 EC2에 재배포.
- 현재는 단일 EC2에 애플리케이션 + MySQL + Redis를 함께 기동. (개선 계획은 §6)

---

## 5. 주요 사용자 시나리오

### 5.1 판매 → 구매 → 수령

1. 판매자 A가 인벤토리에서 아이템을 골라 수량 10개, 총액 5,000머니, 24시간 만료로 등록한다.
2. 구매자 B가 거래소 목록에서 검색해 상품을 찾고 상세를 확인한다.
3. B가 판매자와 채팅으로 문의한다. (선택)
4. B가 구매한다. 잔액에서 5,000머니가 차감되고 상품은 `SOLD`가 된다.
5. A의 수령 대기함에 5,000머니가, B의 수령 대기함에 아이템 10개가 생긴다.
6. 각자 수령하면 지갑/인벤토리에 반영되고 상품은 `CLAIMED`가 된다.
7. 제한 시간 내에 수령하지 않으면 해당 대기 항목은 소멸한다.

### 5.2 선착순 쿠폰 이벤트

1. 관리자가 "총 100개, 10,000머니" 선착순 쿠폰 정책을 만든다.
2. 이벤트 시작과 함께 수천 명이 동시에 발급을 신청한다.
3. 정확히 100명만 발급에 성공하고, 나머지는 품절 응답을 받는다. 중복 신청자도 1개만 받는다.
4. 발급받은 회원이 쿠폰을 사용하면 지갑에 10,000머니가 즉시 증가하고 이력이 남는다.
5. 유효기간이 지난 미사용 쿠폰은 다음 날 자동으로 만료 처리된다.

### 5.3 판매 취소

1. 판매자가 `SELLING` 상태의 상품을 취소한다.
2. 아이템은 즉시 인벤토리로 돌아가지 않고 수령 대기함에 들어간다.
3. 판매자가 수령해야 인벤토리에 복구된다. 제한 시간을 넘기면 소멸한다.

---

## 6. 알려진 제약과 향후 계획

| 구분 | 현재 | 향후 |
|---|---|---|
| 인프라 | 단일 EC2에 App/MySQL/Redis 동거 | MySQL → RDS, Redis → ElastiCache 분리 |
| Redis 구성 | 단일 노드 (클러스터 모드에서 `psubscribe` 미지원으로 회귀) | pub/sub 대체 방식 검토 후 클러스터 재도전 |
| API 버전 | 실험용 V1~V5가 모두 노출됨 | 실험 종료 후 최종 버전만 남기고 정리 |
| 테스트 | CI에서 테스트를 건너뜀(외부 인프라 의존) | Testcontainers 기반으로 CI 내 테스트 활성화 |
| 낙관적 락 | `dev`에 미포함 (`feat/coupon-lock` 브랜치) | 통합 여부 결정 |
| 가상 이메일 | 학습용 단순화 정책 | 실무 정책(이메일 수집 동의 흐름)으로 대체 |
| 알림 | 없음 | 수령 만료 임박 알림 등 |
